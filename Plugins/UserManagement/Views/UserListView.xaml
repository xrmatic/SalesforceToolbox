<UserControl x:Class="SalesforceToolbox.Plugin.UserManagement.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <DockPanel Grid.Row="0" Margin="8,8,8,4">
            <TextBlock Text="User Management" FontSize="18" FontWeight="SemiBold"
                       VerticalAlignment="Center" DockPanel.Dock="Left" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding RefreshCommand}" ToolTip="Refresh"
                        Style="{DynamicResource MahApps.Styles.Button.Square}"
                        Margin="0,0,4,0" Width="32" Height="32">
                    <iconPacks:PackIconMaterial Kind="Refresh" Width="16" Height="16" />
                </Button>
                <Button Command="{Binding ExportCsvCommand}" Content="Export CSV"
                        Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                        Height="32" Padding="8,0" />
            </StackPanel>
        </DockPanel>

        <!-- Search -->
        <mah:MetroTextBox Grid.Row="1" Margin="8,4"
                          mah:TextBoxHelper.Watermark="Search users by name, username, or email..."
                          mah:TextBoxHelper.ClearTextButton="True"
                          Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Loading overlay -->
        <mah:ProgressRing Grid.Row="2" IsActive="{Binding IsLoading}"
                          Width="60" Height="60"
                          Panel.ZIndex="10"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />

        <!-- Data grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredUsers}"
                  SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Margin="8,4">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180" />
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="200" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="60" />
                <DataGridTextColumn Header="Profile" Binding="{Binding Profile.Name}" Width="140" />
                <DataGridTextColumn Header="User Type" Binding="{Binding UserType}" Width="100" />
                <DataGridTextColumn Header="Last Login" Binding="{Binding LastLoginDate, StringFormat=\{0:yyyy-MM-dd\}}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}"
                   Margin="8,4" FontSize="11"
                   Foreground="{DynamicResource MahApps.Brushes.Gray3}" />
    </Grid>
</UserControl>
