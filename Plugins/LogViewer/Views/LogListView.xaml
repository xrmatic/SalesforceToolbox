<UserControl x:Class="SalesforceToolbox.Plugin.LogViewer.Views.LogListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Toolbar spans both columns -->
        <DockPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="8,8,8,4">
            <TextBlock Text="Apex Log Viewer" FontSize="18" FontWeight="SemiBold"
                       VerticalAlignment="Center" DockPanel.Dock="Left" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding RefreshCommand}" ToolTip="Refresh"
                        Style="{DynamicResource MahApps.Styles.Button.Square}"
                        Margin="0,0,4,0" Width="32" Height="32">
                    <iconPacks:PackIconMaterial Kind="Refresh" Width="16" Height="16" />
                </Button>
                <Button Command="{Binding DeleteAllLogsCommand}" Content="Delete All"
                        Style="{DynamicResource MahApps.Styles.Button.Square}"
                        Height="32" Padding="8,0" />
            </StackPanel>
        </DockPanel>

        <!-- Log list -->
        <ListBox Grid.Row="1" Grid.Column="0"
                 ItemsSource="{Binding Logs}"
                 SelectedItem="{Binding SelectedLog, Mode=TwoWay}"
                 BorderThickness="0,1,1,1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding LogUser.Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Operation}" FontSize="11"
                                       Foreground="{DynamicResource MahApps.Brushes.Gray3}" />
                            <TextBlock FontSize="11" Foreground="{DynamicResource MahApps.Brushes.Gray3}">
                                <Run Text="{Binding StartTime, StringFormat=\{0:MM/dd HH:mm\}}" />
                                <Run Text=" | " />
                                <Run Text="{Binding LogLength}" />
                                <Run Text=" bytes" />
                            </TextBlock>
                        </StackPanel>
                        <Button Grid.Column="1"
                                Command="{Binding DataContext.DeleteLogCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}"
                                Style="{DynamicResource MahApps.Styles.Button.Square}"
                                Width="24" Height="24" VerticalAlignment="Top"
                                ToolTip="Delete log">
                            <iconPacks:PackIconMaterial Kind="Delete" Width="12" Height="12" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Log content viewer -->
        <ScrollViewer Grid.Row="1" Grid.Column="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <TextBox Text="{Binding LogContent, Mode=OneWay}"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="11"
                     TextWrapping="NoWrap"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="8" />
        </ScrollViewer>

        <!-- Status bar spans both columns -->
        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding StatusMessage}"
                   Margin="8,4" FontSize="11"
                   Foreground="{DynamicResource MahApps.Brushes.Gray3}" />

        <!-- Loading overlay -->
        <mah:ProgressRing Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Grid.ColumnSpan="2"
                          IsActive="{Binding IsLoading}"
                          Width="60" Height="60"
                          Panel.ZIndex="10"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</UserControl>
